package step01.api;

import java.time.Duration;
import java.time.LocalDateTime;
import java.time.Period;
import java.time.ZonedDateTime;

public class Ex03Time {
	public static void main(String[] args) {
		// LocalDate, LocalTime
//		
//		LocalDate nowDate = LocalDate.now();
//		LocalTime nowTime = LocalTime.now();
//		System.out.println(nowDate.getDayOfMonth());
//		System.out.println(nowTime);
//		
//		LocalDateTime nowDateTime = LocalDateTime.now();
//		System.out.println(nowDateTime);
//		
//		// LocalDate => Date : valueOf
//		LocalDate localDate = LocalDate.now();
//		Date changedDate = Date.valueOf(localDate);
//		System.out.println(changedDate);
//		
//		
//	    // with
//		System.out.println(localDate.withYear(2024).withMonth(12).withDayOfMonth(25));
		
		
		
		
		
		// 현재 날짜와 시간
        LocalDateTime currentDateTime = LocalDateTime.now();
        System.out.println("Current DateTime: " + currentDateTime);

        // 3시간 30분 후의 날짜와 시간
        LocalDateTime futureDateTime = currentDateTime.plusHours(3).plusMinutes(30);
        System.out.println("Future DateTime: " + futureDateTime);

        // 두 날짜와 시간 간의 차이 계산
        Duration duration = Duration.between(currentDateTime, futureDateTime);
        System.out.println("Duration: " + duration.toHours() + " hours, " +
                duration.toMinutes() + " minutes");

				// 현재 날짜와 시간 (특정 시간대)
				ZonedDateTime currentZonedDateTime = ZonedDateTime.now();
        System.out.println("Current ZonedDateTime: " + currentZonedDateTime);

        // 2년 3개월 후의 날짜와 시간 (특정 시간대)
        ZonedDateTime futureZonedDateTime = currentZonedDateTime.plus(Period.ofYears(2).plusMonths(3));
        System.out.println("Future ZonedDateTime: " + futureZonedDateTime);

        // 두 날짜와 시간 간의 차이 계산
        Duration duration2 = Duration.between(currentZonedDateTime, futureZonedDateTime);
        System.out.println("Duration: " + duration2.toDays() + " days, " +
                duration2.toHours() + " hours");
		
		
		
		

	}
}
